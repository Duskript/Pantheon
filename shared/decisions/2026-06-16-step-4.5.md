# Decisions — 2026-06-16 (Step 4.5 follow-up)

## 03:50Z — Step 4.5 Brief 2 backup created (operator prep for re-dispatch)

Step 4.5 Brief 2 was blocked at pre-flight V4 (no backup existed at the path pinned in the Step 4.5 plan YAML, lines 86-88). Per the operator-call "Option A" (create backup, then re-run Brief 2), the full bundle for each of the 6 hephaestus `.archive/` entries was mirrored to `~/athenaeum/Codex-God-hephaestus/.archive/<name>/`.

**Backup contents (120 KB total, 11 files across 6 dirs):**

| Entry | Files | Bytes | Notes |
|---|---|---|---|
| capture-idea | 2 | 5,809 | SKILL.md + `scripts/add-idea.py` (working executable) |
| js-regex-escaping | 1 | 1,642 | SKILL.md only (true to Brief 1 expectation) |
| pantheon-god-bot-setup | 2 | 16,616 | SKILL.md + `references/apollo-bot-setup-example.md` |
| pantheon-mcp-server | 1 | 13,903 | SKILL.md only (true to Brief 1 expectation) |
| pantheon-system-migration | 3 | 9,388 | SKILL.md + `references/{export,install}-script.md` |
| pantheon-wsl-networking | 2 | 10,294 | SKILL.md + `references/remote-work-constraints.md` |

**Brief 1's OBSOLETE dispositions stand.** The bundle content (especially `add-idea.py`) was not factored into Brief 1's classification, but the curator's 2026-05-06 demoted twins in `pantheon-god-architecture/references/` cover the documentation; the executable is the only artifact with no canonical twin. Per the operator's "A" call, the backup preserves it anyway — net effect is zero data loss, full reversibility if the dispositions need to be revisited.

**Why backup BEFORE rm (not "delete in place"):**
- The plan YAML's reversibility claim (line 86) was aspirational, not verified. Future plans need a real pre-flight that actually `ls` the reversibility path before committing to a destructive op.
- The destructive-op-checklist skill (Marvin, 2026-06-15 incident) explicitly requires pre-flight V1-V4 to all pass. V4 caught this before any data was lost — the system worked.
- 120 KB is trivial; the marginal cost of preservation is zero. The marginal cost of a wrong "delete in place" call is the irreversible loss of `add-idea.py` (a working tool) and 4 reference markdown files (curator work product that took time to produce).

**Next:** Step 4.5 Brief 2 v2 re-dispatched with backup path verified. Pre-flight V4 will now pass.

## 03:50Z — Plan YAML reversibility path needs a future fix

The Step 4.5 plan YAML reversibility path (`~/athenaeum/Codex-God-hephaestus/.archive/`, line 86) did not exist when Marvin ran pre-flight V4. The path was a "should be there" assumption from the operator when the plan was authored. Lesson: future plan YAMLs that pin reversibility paths need a "verify backup exists" gate in the build, or the plans need to be flagged as "reversibility TBD until backup is created."

**Action item for Phase 4 review (4.final):** the build plan format should require either a real backup path (verified at plan-write time) or an explicit "no backup" disposition flag, so worker pre-flight doesn't fail late.

**No data lost. Brief 1 dispositions unchanged. Step 4.5 Brief 2 v2 ready to re-dispatch.**

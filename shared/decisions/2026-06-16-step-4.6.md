# 2026-06-16 — Step 4.6 closure (YAML guardrails + workflow validator)

**Context:** Step 4.6 was marked "deferrable" in the original Phase 4 plan (the runtime engine regex already catches the sovereign-outbound breach shape at publish time). Operator un-deferred on 2026-06-16 as part of the substrate-hardening sequence (4.6 → 4.9 → 4.final). Defense-in-depth: catch the gap at workflow LOAD time, not just at publish time.

## What shipped

3 briefs, all SHIP'd:

### Brief 1 (1-line YAML edit)
- deploy-feature.yaml:53 — added `operator_approval_required: true` to the `notify-enterprise` step. Locks the contract at the workflow YAML layer for the workflow that breached in 2026-06-15.

### Brief 2 (validator module + tests + CLI + 2 production fixes)
- pantheon/conductor/v2/workflow_validator.py (NEW, 3.5K)
  - `is_sovereign_outbound(subject)` — thin wrapper around `SOVEREIGN_OUTBOUND_RE` from engine.py:163
  - `validate_workflow(workflow)` → list[str] — returns violations, doesn't raise
  - `validate_workflow_file(path)` — loads YAML, validates
  - `validate_workflow_dir(dirpath, skip_glob='bridge-test-*')` — walks directory
  - `WorkflowValidationError(ValueError)` — exception class
- pantheon/conductor/v2/engine.py — added 3 things:
  - `operator_approval_required: bool = False` to `WorkflowStep` dataclass (line 529)
  - `operator_approval_required=bool(s.get("operator_approval_required", False))` to `_step_from_dict` (line 622)
  - Lazy-import validation hook in `Workflow.from_dict` (line 571-574) — raises WorkflowValidationError on violations
- pantheon/conductor/scripts/validate-workflows.py (NEW, 1.5K) — CLI: exit 0 clean, 1 violations, 2 missing dir
- pantheon/conductor/v2/tests/test_workflow_validator.py (NEW, 16.1K, 21 tests) — all pass
- 2 unauthorized-but-correct production YAML fixes (caught by Brief 2, not in original brief):
  - cross-pantheon-deploy.yaml:19 — added `operator_approval_required: true` to notify-enterprise step
  - sovereign-publish-tallon-correction.yaml:notify-tallon — added `operator_approval_required: true` to the retraction publish step
  - Both were caught because they had sovereign subjects without the new field — the validator would have refused to load them. **Brief 1's assumption that "the others have non-sovereign nats_publish" was wrong** — Brief 2 caught it on the first validation pass.

### Brief 3 (closure — this brief)
- Full v2 suite: 255/1-skip/0-fail (was 234/1/0 after 4.8; +21 validator tests)
- 21/21 validator tests pass
- CLI: exit 0, "OK: all workflows in /home/konan/pantheon/conductor/workflows pass sovereign-outbound validation"
- Bypass hand-test: WorkflowValidationError raised with clear message: `step 'leak' (subject='subspace.konan.outgoing.tallon') is a sovereign outbound and MUST have 'operator_approval_required: true'`
- Negative hand-test: 5 production workflows load cleanly (bug-fix 4 steps, cross-pantheon-deploy 3 steps, deploy-feature 6 steps, morning-briefing 6 steps, sovereign-publish-tallon-correction 1 step)

## Discrepancy caught + how

The original Brief 2 (my draft) said: "all 5 production workflows would pass because the others have non-sovereign nats_publish." This was incorrect. Brief 2's first test run would have failed on cross-pantheon-deploy.yaml and sovereign-publish-tallon-correction.yaml. Hephaestus caught it on his own and added the missing field to both. The validator was then re-run and passed.

**Lesson:** the brief's "expectation of clean state" was wrong because Brief 1 only locked deploy-feature.yaml. Any future "load all workflows" check needs to account for the WHOLE workflows directory, not just the ones mentioned in the brief. **Cited in the 4.final review (section 1_holes: implicit scope calls).**

## Reversibility

Trivial. Revert the 5-line addition to `Workflow.from_dict` (engine.py:571-574), revert the `operator_approval_required` field on `WorkflowStep` and `_step_from_dict`, delete `workflow_validator.py` + `validate-workflows.py` + `test_workflow_validator.py`, revert the 3 `operator_approval_required: true` additions to the 3 production workflows (deploy-feature, cross-pantheon-deploy, sovereign-publish-tallon-correction). Zero data state changes.

## What comes next

- **Step 4.9 (cli_tool step type)** — dispatches immediately after this brief lands. 3 briefs total, owner Marvin.
- **Step 4.final (Phase 4 closure review)** — runs after 4.9 SHIPs cleanly. 5-section review at `reviews/phase-4-quarantine-sovereign-final-review.md`. The two latent engine bugs logged from Step 4.8 (declared_output parameter, single-branch sub-workflow premature status) get fixed or explicitly deferred in 4.final.

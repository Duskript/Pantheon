# 2026-06-17 — Phase 4.0b commit decision (PR #35)

## Decision

The Phase 4.0b work (api_server + auth on 3 surfaces, task `t_e145441a`)
was developed on `feature/a2-websocket-live-stream` but left uncommitted.
The shipped doc explicitly punted the commit decision to Hephaestus.

**Chose: Option B (operator's call).** New branch `phase-4.0b-api-server`
based on `origin/main` (b03c06c), single commit, PR opened.

## What landed

- Branch: `phase-4.0b-api-server`
- Commit: `a389f194`
- PR: https://github.com/Duskript/Pantheon/pull/35
- Files: 7 (3,101 insertions, 7 deletions)
  - `conductor/v2/api_server.py` (NEW, 903 LOC)
  - `conductor/v2/auth.py` (NEW, 274 LOC)
  - `conductor/v2/live_stream.py` (NEW, 515 LOC — rebuilt with auth)
  - `conductor/v2/webhook.py` (MODIFIED, +38/-7)
  - `conductor/v2/tests/test_api_server.py` (NEW, 771 LOC)
  - `conductor/v2/tests/test_live_stream.py` (NEW, 539 LOC)
  - `shared/active/conductor-ui-phase-4-0b-shipped.md` (NEW, 61 LOC)

## Why a fresh branch, not commit on feature/a2-websocket-live-stream

The source branch had:
- 1 unrelated committed delta (uce-v0.3 D1: connector library,
  11 files, ~1,900 LOC)
- ~20 unrelated uncommitted modifications (cli_tool, engine, service,
  cron, webui, hermes-agent submodule, .gitignore, multiple shared/
  files, etc.)
- 3 unresolved merge conflicts (feature-list.md, webui/api/kanban_bridge.py,
  webui/static/index.html)

A clean PR for review meant a fresh branch off `origin/main` with just
the 4.0 files staged. The connectors D1 work and the dirty working
tree stay on their own branch — separate kanban stories, separate PRs.

## Mechanics

- `git worktree add /tmp/phase-4.0b-wt -b phase-4.0b-api-server origin/main`
  (worktree was needed because the main worktree had unresolved
  merge conflicts blocking `git checkout -b`)
- Copied 6 source files from `~/pantheon/conductor/v2/` and 1 doc from
  `~/pantheon/shared/active/` to the worktree
- `git add` the explicit file list (no `git add -A` — too risky given
  the source branch's dirty tree)
- Single commit with descriptive message documenting what ships, the
  verification evidence, the branch provenance, and the operator-
  locked QA gate
- `git push origin phase-4.0b-api-server` + `gh pr create`

## Verification (independently re-run on the clean branch)

- `pytest test_api_server.py test_live_stream.py test_webhook.py` →
  **49/49 pass** (4.93s)
- `pytest conductor/v2/tests/ --ignore=test_backbone_e2e.py` →
  234/239 pass (5 unrelated fails, 1 expected skip)
- 2 of the 5 fails (`1.1.0-superseded` in test_engine +
  test_schedule_cron_binding) **reproduce on origin/main without the
  4.0 overlay** — confirmed pre-existing, not introduced by this commit
- 2 of the 5 fails (test_service state-pollution) pass when the test
  module runs in isolation — environmental, not 4.0

## QA gate status

- **Tier-1 (Hephaestus, in-session verify):** ✅ done in this commit
- **Tier-2 (GPT-5.5 Ponytail):** ❌ not yet run

Per the operator-locked 2026-06-17 QA flow (Marvin/Iris → Hephaestus
tier-1 → GPT-5.5 Ponytail gate → ACCEPT/RETURN), the PR is NOT
ready to merge until Tier-2 PASS lands. Recommend:
1. Run Ponytail QA on the diff (use gpt55-ponytail-qa skill)
2. If ACCEPT: merge to main
3. If RETURN/ESCALATE: address findings, push a fix commit, re-run

## Unblocks

Phase 4.2 (SDK mount in /editor/$id) and Phase 3.8 (editor↔kanban
cross-route) are now unblocked — both have 4.0 as a precondition in
the v1.2 plan.

## Follow-ups parked (not in this commit)

- The 2 `1.1.0-superseded` test failures — separate triage PR
- The `api_server.pre-4.0a-full.py` backup file on the source branch
  — can be deleted, redundant
- Tier-2 Ponytail QA — gating step before merge

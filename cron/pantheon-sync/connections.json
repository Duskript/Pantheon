{
  "_note": "Active Pantheon sync connections. Each uses a 'provider' key that maps to a registered adapter.",
  "connections": [
    {
      "id": "gmail-primary",
      "provider": "gmail",
      "enabled": false,
      "sync_interval_minutes": 20,
      "daily_budget": 500,
      "_note": "Enable after T14 OAuth wiring. Requires Composio BYOK credentials."
    },
    {
      "id": "github-primary",
      "provider": "github",
      "enabled": false,
      "sync_interval_minutes": 20,
      "daily_budget": 500,
      "_note": "Enable after T14 OAuth wiring."
    },
    {
      "id": "slack-primary",
      "provider": "slack",
      "enabled": false,
      "sync_interval_minutes": 20,
      "daily_budget": 500,
      "_note": "Enable after T14 OAuth wiring."
    }
  ]
}